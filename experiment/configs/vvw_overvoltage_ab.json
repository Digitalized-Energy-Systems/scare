{
  "$comment": [
    "VVW prototype A/B on the LV-S over-voltage regime.  Two paired arms on",
    "identical (grid, seed, scenario, failure-draw): baseline {} (default scare —",
    "Q(U) droop capped at the VDE-AR-N 4105 cos-phi displacement envelope) vs",
    "{enable_vvw_coordination:true} (each inverter uses its FULL apparent-power",
    "capability circle under voltage support).  The eval planner's seed excludes",
    "the ablation index, so both arms share the same failure draw -> clean paired",
    "comparison.  Scenario is pv_peak (sunny-midday reverse-flow over-voltage,",
    "the VDE-AR-N 4105 regime) on simbench_lv_small, the grid where the static",
    "5.7x PV-to-load condition pushes vm_pu past 1.05.  30 s horizon so the",
    "curtailment-auction compounding has time to push through (5 s is too short).",
    "Question: does VVW clear over-voltage with LESS active PV curtailment, or",
    "does uncoordinated everyone-max reactive backfire (the earlier 2-seed read)?"
  ],
  "name": "vvw_overvoltage_ab",
  "out_root": "experiment/_runs/eval",
  "simulation_duration_s": 30.0,
  "task_timeout_s": 2400.0,
  "failure_delay_s_max": 2.0,
  "write_timeseries": true,
  "write_trajectories": true,
  "timestamp_dir": true,
  "defaults": {
    "runs_per_grid": 8,
    "failure_lambda": 0.5,
    "max_failures": 1
  },
  "experiments": [
    {
      "$comment": "VVW off (default scare) vs VVW on, paired per seed.",
      "name": "vvw_pv_peak",
      "grids": ["simbench_lv_small"],
      "n_seeds": 8,
      "variants": ["scare"],
      "ablations": [
        {},
        { "enable_vvw_coordination": true }
      ],
      "scenarios": [
        {
          "kind": "pv_peak",
          "gen_scale": 1.5,
          "load_scale": 0.4,
          "slack_vm_pu": 1.04,
          "priority_assignment": "skewed"
        }
      ]
    }
  ],
  "slurm": {
    "partition": "rosa_express.p",
    "time": "02:00:00",
    "mem": "16G",
    "cpus": 1,
    "max_concurrent": 64
  }
}
