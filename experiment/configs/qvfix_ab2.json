{
  "$comment": [
    "Second code-change A/B: isolates review fixes #2-#5 (baseline = post-#1",
    "snapshot, fixed = all fixes). Runs with enable_qv_auction_coordination ON so",
    "the relief-sensitivity fixes (#2 dV/dQ lag pairing, #3 q_max-scaled",
    "calibration gate) and #4 (coordination implies generation-priority for",
    "over-voltage, so it curtails generation not loads) all engage. Same LV-S",
    "pv_peak scenario and seeds as qvfix_ab for comparability."
  ],
  "name": "qvfix_ab2",
  "out_root": "experiment/_runs/eval",
  "simulation_duration_s": 30.0,
  "task_timeout_s": 1200.0,
  "failure_delay_s_max": 2.0,
  "write_timeseries": true,
  "write_trajectories": false,
  "timestamp_dir": false,
  "defaults": { "runs_per_grid": 12, "failure_lambda": 0.5, "max_failures": 1 },
  "experiments": [
    {
      "name": "qvfix_pv_peak_coord",
      "grids": ["simbench_lv_small"],
      "n_seeds": 12,
      "variants": ["scare"],
      "ablations": [ { "enable_qv_auction_coordination": true } ],
      "scenarios": [
        {
          "kind": "pv_peak",
          "gen_scale": 1.5,
          "load_scale": 0.4,
          "slack_vm_pu": 1.04,
          "priority_assignment": "skewed"
        }
      ]
    }
  ],
  "slurm": { "partition": "rosa_express.p", "time": "02:00:00", "mem": "16G", "cpus": 1, "max_concurrent": 64 }
}
