{
  "$comment": [
    "Reproduction of eval_full task 000000 (functional_baseline / simbench_lv / scare,",
    "scenario clean max_failures=3 skewed slack_budget_pct=0.3, seed 0) which timed out.",
    "Trimmed to a single task; task_timeout_s reduced to 360 so a hang is caught fast.",
    "failure_delay_s_max and defaults match eval_full.json so failure sampling is identical."
  ],
  "name": "repro_timeout000000",
  "out_root": "experiment/_runs/repro",
  "simulation_duration_s": 30.0,
  "task_timeout_s": 360.0,
  "failure_delay_s_max": 2.0,
  "write_timeseries": false,
  "timestamp_dir": false,
  "defaults": {
    "runs_per_grid": 30,
    "failure_lambda": 1.5,
    "max_failures": 4
  },
  "experiments": [
    {
      "name": "functional_baseline",
      "grids": [
        "simbench_lv"
      ],
      "n_seeds": 1,
      "variants": [
        "scare"
      ],
      "scenarios": [
        {
          "kind": "clean",
          "max_failures": 3,
          "priority_assignment": "skewed",
          "slack_budget_pct": 0.3
        }
      ]
    }
  ],
  "slurm": {
    "partition": "rosa_express.p",
    "time": "02:00:00",
    "mem": "16G",
    "cpus": 1,
    "max_concurrent": 64,
    "aggregate": false
  }
}
