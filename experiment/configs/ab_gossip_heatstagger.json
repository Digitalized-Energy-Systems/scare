{
  "$comment": "Paired code A/B for the gossip gen-curtail-lock bypass fix + heat frontier tier-staggering. Arm A = HEAD (stashed fixes), arm B = fixed tree; same config, deterministic seeds. LV-S exercises the PV-surplus curtailment race; LV-M exercises heat tier inversion (worst grid in eval_full_20260703).",
  "name": "ab_gossip_heatstagger",
  "out_root": "experiment/_runs/eval",
  "simulation_duration_s": 30.0,
  "task_timeout_s": 1200.0,
  "failure_delay_s_max": 2.0,
  "write_timeseries": false,
  "write_trajectories": false,
  "timestamp_dir": true,
  "defaults": { "runs_per_grid": 16, "failure_lambda": 0.5, "max_failures": 3 },
  "experiments": [
    {
      "name": "lvs",
      "grids": ["simbench_lv_small"],
      "n_seeds": 16,
      "variants": ["scare"],
      "ablations": [ {} ],
      "scenarios": [
        { "kind": "clean", "max_failures": 3, "priority_assignment": "skewed", "slack_budget_pct": 0.45 }
      ]
    },
    {
      "name": "heat",
      "grids": ["simbench_lv_medium"],
      "n_seeds": 16,
      "variants": ["scare"],
      "ablations": [ {} ],
      "scenarios": [
        { "kind": "clean", "max_failures": 3, "priority_assignment": "skewed", "slack_budget_pct": 0.45 }
      ]
    }
  ],
  "slurm": { "partition": "rosa_express.p", "time": "02:00:00", "mem": "16G", "cpus": 1, "max_concurrent": 64 }
}
